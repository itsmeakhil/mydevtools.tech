export interface HashResponse {
    success: boolean
    hash?: string
    error?: string
  }
  
  export interface CompareResponse {
    success: boolean
    isMatch?: boolean
    error?: string
  }
  
  